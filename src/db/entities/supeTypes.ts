export enum ESupeEntityType {
  SALESMAN = 'salesman',
  RETAILER = 'retailer',
  SKU = 'sku',
  BEAT = 'beat',
  DISTRIBUTOR = 'distributor'
}

export enum ETargetStatus {
  ACTIVE = 'active',
  PAUSED = 'paused',
  COMPLETED = 'completed'
}

export enum EGoalStatus {
  ACTIVE = 'active',
  COMPLETED = 'completed',
  PAUSED = 'paused',
  ARCHIVED = 'archived'
}

export enum ESignalScopeType {
  NATIONAL = 'national',
  ZONE = 'zone',
  REGION = 'region'
}
